/**
 * Landing content for Mero Blocks.
 *
 * GENERATED by `pnpm landing:generate` from scripts/landing/apps.config.mjs
 * plus this app's [package.metadata.calimero] table. Do not edit by hand —
 * `pnpm landing:check` fails CI on drift. Change the copy in apps.config.mjs.
 *
 * `name`, `packageId` and `tagline` come from the Cargo metadata the registry
 * publishes from, so this page and the registry listing cannot diverge.
 */
import { CloudX, Cube3DStacked, Monitor, Wifi, WifiOff } from '@calimero-network/mero-icons';

import Animation from './animation';
import type { LandingConfig } from './landingTypes';

export const CONFIG: LandingConfig = {
  name: "Mero Blocks",
  packageId: "com.calimero.mero-blocks",
  tagline: "Minecraft-style P2P voxel sandbox. The world is a Calimero context: seed + block-edit diff + player presence, no game server.",
  dir: "mero-blocks",
  markSrc: "/favicon.svg",
  iconSrc: '/icon-512.png',
  availability: "web+desktop",
  playableOffline: true,
  trust: ["No game server", "Deterministic world", "Plays offline"],
  explainer: [
    "A Minecraft-style voxel sandbox you can build in with other people, hosted by nobody. The world is not a server you connect to — it is a Calimero context holding a seed plus the diff of every block anyone has edited.",
    "Because the terrain generator is deterministic, the same seed and the same diff produce the same world on every peer, so all that has to replicate is the edits. And if you just want to dig around on your own, it runs with no node at all.",
  ],
  features: [
    {
      icon: CloudX,
      title: "No game server",
      body: "The world is a seed plus a block-edit diff, in a context you own. Nothing to host, nothing to pay for.",
    },
    {
      icon: WifiOff,
      title: "Plays fully offline",
      body: "No node required. The world persists to localStorage and is yours alone until you want company.",
    },
    {
      icon: Cube3DStacked,
      title: "Deterministic terrain",
      body: "The same seed builds the same world on every peer, so only the edits need to travel.",
    },
    {
      icon: Wifi,
      title: "See other players",
      body: "Presence replicates alongside the edits, so you watch people build in real time.",
    },
    {
      icon: Monitor,
      title: "Runs in a browser",
      body: "A Three.js renderer over a pure-TypeScript engine. Nothing to install to start playing.",
    },
  ],
  animation: Animation,
};
