/** @generated by @calimero/abi-codegen — DO NOT EDIT. */

import {
  MeroJs,
} from '@calimero-network/mero-js';

// Generated types

export interface ContextDetails {
  context_id: CalimeroBytes;
  context_name: string;
  owner: CalimeroBytes;
  is_private: boolean;
  participant_count: number;
  participants: ParticipantInfo[];
  document_count: number;
  created_at: number;
}

export interface ContextMetadata {
  context_id: CalimeroBytes;
  context_name: string;
  role: ParticipantRole;
  joined_at: number;
  private_identity: CalimeroBytes;
  shared_identity: CalimeroBytes;
}

export interface DocumentChunk {
  text: string;
  embedding: number[];
  start_position: number;
  end_position: number;
}

export interface DocumentInfo {
  id: string;
  name: string;
  hash: string;
  uploaded_by: CalimeroBytes;
  uploaded_at: number;
  status: DocumentStatus;
  pdf_blob_id: CalimeroBytes;
  size: number;
  embeddings: number[] | null;
  extracted_text: string | null;
  chunks: DocumentChunk[] | null;
}

export interface DocumentSignature {
  signer: CalimeroBytes;
  signed_at: number;
}

export type DocumentStatus = 'Pending' | 'PartiallySigned' | 'FullySigned';

export interface IdentityMapping {
  private_identity: CalimeroBytes;
  shared_identity: CalimeroBytes;
  context_id: CalimeroBytes;
  created_at: number;
}

export interface MeroSignEvent_ContextJoined {
  context_id: string;
  context_name: string;
}

export interface MeroSignEvent_ContextLeft {
  context_id: string;
}

export interface MeroSignEvent_DocumentDeleted {
  id: string;
}

export interface MeroSignEvent_DocumentSigned {
  document_id: string;
  signer: CalimeroBytes;
}

export interface MeroSignEvent_DocumentUploaded {
  id: string;
  name: string;
  uploaded_by: CalimeroBytes;
}

export interface MeroSignEvent_ParticipantInvited {
  user_id: CalimeroBytes;
  role: ParticipantRole;
}

export interface MeroSignEvent_ParticipantJoined {
  user_id: CalimeroBytes;
}

export interface MeroSignEvent_ParticipantLeft {
  user_id: CalimeroBytes;
}

export interface MeroSignEvent_ParticipantPermissionChanged {
  user_id: CalimeroBytes;
  permission: PermissionLevel;
}

export interface MeroSignEvent_SignatureCreated {
  id: number;
  name: string;
  size: number;
}

export interface MeroSignEvent_SignatureDeleted {
  id: number;
}

export interface MeroSignState {
  is_private: boolean;
  owner: CalimeroBytes;
  context_name: string;
  signatures: Record<string, SignatureRecord>;
  joined_contexts: Record<string, ContextMetadata>;
  identity_mappings: Record<string, IdentityMapping>;
  signature_count: number;
  participants: CalimeroBytes[];
  documents: Record<string, DocumentInfo>;
  document_signatures: Record<string, DocumentSignature[]>;
  permissions: Record<string, PermissionCell>;
  consents: Record<string, boolean>;
}

export interface ParticipantInfo {
  user_id: CalimeroBytes;
  permission_level: PermissionLevel;
}

export type ParticipantRole = 'Owner' | 'Signer' | 'Viewer' | 'Unknown';

export interface PermissionCell {
  level: PermissionLevel;
}

export type PermissionLevel = 'Read' | 'Sign' | 'Admin';

export interface SignatureRecord {
  id: number;
  name: string;
  blob_id: CalimeroBytes;
  size: number;
  created_at: number;
}












export type AbiEvent =
  | { name: "ContextJoined"; payload: MeroSignEvent_ContextJoined }
  | { name: "ContextLeft"; payload: MeroSignEvent_ContextLeft }
  | { name: "DocumentDeleted"; payload: MeroSignEvent_DocumentDeleted }
  | { name: "DocumentSigned"; payload: MeroSignEvent_DocumentSigned }
  | { name: "DocumentUploaded"; payload: MeroSignEvent_DocumentUploaded }
  | { name: "ParticipantInvited"; payload: MeroSignEvent_ParticipantInvited }
  | { name: "ParticipantJoined"; payload: MeroSignEvent_ParticipantJoined }
  | { name: "ParticipantLeft"; payload: MeroSignEvent_ParticipantLeft }
  | { name: "ParticipantPermissionChanged"; payload: MeroSignEvent_ParticipantPermissionChanged }
  | { name: "SignatureCreated"; payload: MeroSignEvent_SignatureCreated }
  | { name: "SignatureDeleted"; payload: MeroSignEvent_SignatureDeleted }
;


/**
 * Utility class for handling byte conversions in Calimero
 */
export class CalimeroBytes {
  private data: Uint8Array;

  constructor(input: string | number[] | Uint8Array) {
    if (typeof input === "string") {
      // Hex string
      this.data = new Uint8Array(
        input.match(/.{1,2}/g)?.map((byte) => parseInt(byte, 16)) || []
      );
    } else if (Array.isArray(input)) {
      // Number array
      this.data = new Uint8Array(input);
    } else {
      // Uint8Array
      this.data = input;
    }
  }

  toArray(): number[] {
    return Array.from(this.data);
  }

  toUint8Array(): Uint8Array {
    return this.data;
  }

  static fromHex(hex: string): CalimeroBytes {
    return new CalimeroBytes(hex);
  }

  static fromArray(arr: number[]): CalimeroBytes {
    return new CalimeroBytes(arr);
  }

  static fromUint8Array(bytes: Uint8Array): CalimeroBytes {
    return new CalimeroBytes(bytes);
  }
}

export class MeroSignClient {
  private _mero: MeroJs;
  private _contextId: string;

  constructor(mero: MeroJs, contextId: string) {
    this._mero = mero;
    this._contextId = contextId;
  }

  /**
   * add_participant
   *
   * @intent mutating
   */
  public async addParticipant(params: { user_id_str: string; permission: PermissionLevel }): Promise<void> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'add_participant', argsJson: params });
    return response as void;
  }

  /**
   * create_signature
   *
   * @intent mutating
   */
  public async createSignature(params: { name: string; blob_id_str: string; data_size: number }): Promise<number> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'create_signature', argsJson: params });
    return response as number;
  }

  /**
   * delete_document
   *
   * @intent mutating
   */
  public async deleteDocument(params: { document_id: string }): Promise<void> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'delete_document', argsJson: params });
    return response as void;
  }

  /**
   * delete_signature
   *
   * @intent mutating
   */
  public async deleteSignature(params: { signature_id: number }): Promise<void> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'delete_signature', argsJson: params });
    return response as void;
  }

  /**
   * get_context_details
   *
   * @intent read_only
   */
  public async getContextDetails(params: { context_id_str: string }): Promise<ContextDetails> {
    const response: any = await this._mero.rpc.execute({ contextId: this._contextId, method: 'get_context_details', argsJson: params });
    return (response == null ? null : ({ ...response, context_id: new CalimeroBytes(response['context_id']), owner: new CalimeroBytes(response['owner']), participants: response['participants'].map((item: any) => ({ ...item, user_id: new CalimeroBytes(item['user_id']) })) })) as ContextDetails;
  }

  /**
   * get_context_id
   *
   * @intent read_only
   */
  public async getContextId(): Promise<CalimeroBytes> {
    const response: any = await this._mero.rpc.execute({ contextId: this._contextId, method: 'get_context_id', argsJson: {} });
    return (response == null ? null : new CalimeroBytes(response)) as CalimeroBytes;
  }

  /**
   * get_document_signatures
   *
   * @intent read_only
   */
  public async getDocumentSignatures(params: { document_id: string }): Promise<DocumentSignature[]> {
    const response: any = await this._mero.rpc.execute({ contextId: this._contextId, method: 'get_document_signatures', argsJson: params });
    return (response == null ? null : response.map((item: any) => ({ ...item, signer: new CalimeroBytes(item['signer']) }))) as DocumentSignature[];
  }

  /**
   * get_identity_mapping
   *
   * @intent read_only
   */
  public async getIdentityMapping(params: { context_id_str: string }): Promise<IdentityMapping> {
    const response: any = await this._mero.rpc.execute({ contextId: this._contextId, method: 'get_identity_mapping', argsJson: params });
    return (response == null ? null : ({ ...response, private_identity: new CalimeroBytes(response['private_identity']), shared_identity: new CalimeroBytes(response['shared_identity']), context_id: new CalimeroBytes(response['context_id']) })) as IdentityMapping;
  }

  /**
   * get_shared_identity
   *
   * @intent read_only
   */
  public async getSharedIdentity(params: { context_id_str: string }): Promise<CalimeroBytes> {
    const response: any = await this._mero.rpc.execute({ contextId: this._contextId, method: 'get_shared_identity', argsJson: params });
    return (response == null ? null : new CalimeroBytes(response)) as CalimeroBytes;
  }

  /**
   * get_user_permission
   *
   * @intent read_only
   */
  public async getUserPermission(params: { user_id_str: string }): Promise<PermissionLevel> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'get_user_permission', argsJson: params });
    return response as PermissionLevel;
  }

  /**
   * has_consented
   *
   * @intent read_only
   */
  public async hasConsented(params: { user_id_str: string; document_id: string }): Promise<boolean> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'has_consented', argsJson: params });
    return response as boolean;
  }

  /**
   * init
   */
  public async init(params: { is_private: boolean; context_name: string }): Promise<void> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'init', argsJson: params });
    return response as void;
  }

  /**
   * is_default_private_context
   *
   * @intent read_only
   */
  public async isDefaultPrivateContext(): Promise<boolean> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'is_default_private_context', argsJson: {} });
    return response as boolean;
  }

  /**
   * join_shared_context
   *
   * @intent mutating
   */
  public async joinSharedContext(params: { context_id_str: string; shared_identity_str: string; context_name: string }): Promise<void> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'join_shared_context', argsJson: params });
    return response as void;
  }

  /**
   * leave_shared_context
   *
   * @intent mutating
   */
  public async leaveSharedContext(params: { context_id_str: string }): Promise<void> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'leave_shared_context', argsJson: params });
    return response as void;
  }

  /**
   * list_documents
   *
   * @intent read_only
   */
  public async listDocuments(): Promise<DocumentInfo[]> {
    const response: any = await this._mero.rpc.execute({ contextId: this._contextId, method: 'list_documents', argsJson: {} });
    return (response == null ? null : response.map((item: any) => ({ ...item, uploaded_by: new CalimeroBytes(item['uploaded_by']), pdf_blob_id: new CalimeroBytes(item['pdf_blob_id']) }))) as DocumentInfo[];
  }

  /**
   * list_joined_contexts
   *
   * @intent read_only
   */
  public async listJoinedContexts(): Promise<ContextMetadata[]> {
    const response: any = await this._mero.rpc.execute({ contextId: this._contextId, method: 'list_joined_contexts', argsJson: {} });
    return (response == null ? null : response.map((item: any) => ({ ...item, context_id: new CalimeroBytes(item['context_id']), private_identity: new CalimeroBytes(item['private_identity']), shared_identity: new CalimeroBytes(item['shared_identity']) }))) as ContextMetadata[];
  }

  /**
   * list_participants
   *
   * @intent read_only
   */
  public async listParticipants(): Promise<CalimeroBytes[]> {
    const response: any = await this._mero.rpc.execute({ contextId: this._contextId, method: 'list_participants', argsJson: {} });
    return (response == null ? null : response.map((item: any) => new CalimeroBytes(item))) as CalimeroBytes[];
  }

  /**
   * list_signatures
   *
   * @intent read_only
   */
  public async listSignatures(): Promise<SignatureRecord[]> {
    const response: any = await this._mero.rpc.execute({ contextId: this._contextId, method: 'list_signatures', argsJson: {} });
    return (response == null ? null : response.map((item: any) => ({ ...item, blob_id: new CalimeroBytes(item['blob_id']) }))) as SignatureRecord[];
  }

  /**
   * mark_participant_signed
   *
   * @intent mutating
   */
  public async markParticipantSigned(params: { document_id: string }): Promise<void> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'mark_participant_signed', argsJson: params });
    return response as void;
  }

  /**
   * register_self_as_participant
   *
   * @intent mutating
   */
  public async registerSelfAsParticipant(): Promise<void> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'register_self_as_participant', argsJson: {} });
    return response as void;
  }

  /**
   * remove_participant
   *
   * @intent mutating
   */
  public async removeParticipant(params: { user_id_str: string }): Promise<void> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'remove_participant', argsJson: params });
    return response as void;
  }

  /**
   * resolve_private_identity
   *
   * @intent read_only
   */
  public async resolvePrivateIdentity(params: { shared_identity_str: string }): Promise<CalimeroBytes> {
    const response: any = await this._mero.rpc.execute({ contextId: this._contextId, method: 'resolve_private_identity', argsJson: params });
    return (response == null ? null : new CalimeroBytes(response)) as CalimeroBytes;
  }

  /**
   * search_document_by_embedding
   *
   * @intent read_only
   */
  public async searchDocumentByEmbedding(params: { query_embedding: number[]; document_id: string }): Promise<string> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'search_document_by_embedding', argsJson: params });
    return response as string;
  }

  /**
   * set_consent
   *
   * @intent mutating
   */
  public async setConsent(params: { document_id: string }): Promise<void> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'set_consent', argsJson: params });
    return response as void;
  }

  /**
   * set_participant_permission
   *
   * @intent mutating
   */
  public async setParticipantPermission(params: { user_id_str: string; permission: PermissionLevel }): Promise<void> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'set_participant_permission', argsJson: params });
    return response as void;
  }

  /**
   * sign_document
   *
   * @intent mutating
   */
  public async signDocument(params: { document_id: string; pdf_blob_id_str: string; file_size: number; new_hash: string }): Promise<void> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'sign_document', argsJson: params });
    return response as void;
  }

  /**
   * upload_document
   *
   * @intent mutating
   */
  public async uploadDocument(params: { name: string; hash: string; pdf_blob_id_str: string; file_size: number; embeddings: number[] | null; extracted_text: string | null; chunks: DocumentChunk[] | null }): Promise<string> {
    const response = await this._mero.rpc.execute({ contextId: this._contextId, method: 'upload_document', argsJson: params });
    return response as string;
  }

  /**
   * whoami
   *
   * @intent read_only
   */
  public async whoami(): Promise<CalimeroBytes> {
    const response: any = await this._mero.rpc.execute({ contextId: this._contextId, method: 'whoami', argsJson: {} });
    return (response == null ? null : new CalimeroBytes(response)) as CalimeroBytes;
  }

}