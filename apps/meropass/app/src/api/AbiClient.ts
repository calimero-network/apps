/** @generated by @calimero/abi-codegen — DO NOT EDIT. */

import {
  CalimeroApp,
  Context,
} from '@calimero-network/calimero-client';

// Generated types

export interface AuditLogEntry {
  id: string;
  action: string;
  details: string;
  user_public_key: string;
  timestamp: number;
}

export interface SecretItem {
  id: string;
  name: string;
  secret_type: string;
  data: string;
  tags: string[];
  created_at: number;
  updated_at: number;
  version: number;
  created_by: string;
}




export type AbiEvent =
  | { name: "SecretAdded" }
  | { name: "SecretUpdated" }
  | { name: "SecretDeleted" }
;


/**
 * Utility class for handling byte conversions in Calimero
 */
export class CalimeroBytes {
  private data: Uint8Array;

  constructor(input: string | number[] | Uint8Array) {
    if (typeof input === "string") {
      // Hex string
      this.data = new Uint8Array(
        input.match(/.{1,2}/g)?.map((byte) => parseInt(byte, 16)) || []
      );
    } else if (Array.isArray(input)) {
      // Number array
      this.data = new Uint8Array(input);
    } else {
      // Uint8Array
      this.data = input;
    }
  }

  toArray(): number[] {
    return Array.from(this.data);
  }

  toUint8Array(): Uint8Array {
    return this.data;
  }

  static fromHex(hex: string): CalimeroBytes {
    return new CalimeroBytes(hex);
  }

  static fromArray(arr: number[]): CalimeroBytes {
    return new CalimeroBytes(arr);
  }

  static fromUint8Array(bytes: Uint8Array): CalimeroBytes {
    return new CalimeroBytes(bytes);
  }
}

/**
 * Convert CalimeroBytes instances to arrays for WASM compatibility
 */
function convertCalimeroBytesForWasm(obj: any): any {
  if (obj === null || obj === undefined) {
    return obj;
  }

  if (obj instanceof CalimeroBytes) {
    return obj.toArray();
  }

  if (Array.isArray(obj)) {
    return obj.map(item => convertCalimeroBytesForWasm(item));
  }

  if (typeof obj === "object") {
    const result: any = {};
    for (const [key, value] of Object.entries(obj)) {
      result[key] = convertCalimeroBytesForWasm(value);
    }
    return result;
  }

  return obj;
}

/**
 * Convert arrays back to CalimeroBytes instances from WASM responses
 */
function convertWasmResultToCalimeroBytes(obj: any): any {
  if (obj === null || obj === undefined) {
    return obj;
  }

  if (Array.isArray(obj) && obj.every(item => typeof item === "number")) {
    return new CalimeroBytes(obj);
  }

  if (Array.isArray(obj)) {
    return obj.map(item => convertWasmResultToCalimeroBytes(item));
  }

  if (typeof obj === "object") {
    const result: any = {};
    for (const [key, value] of Object.entries(obj)) {
      result[key] = convertWasmResultToCalimeroBytes(value);
    }
    return result;
  }

  return obj;
}

export class AbiClient {
  private app: CalimeroApp;
  private context: Context;

  constructor(app: CalimeroApp, context: Context) {
    this.app = app;
    this.context = context;
  }

  /**
   * init
   */
  public async init(): Promise<void> {
    const response = await this.app.execute(this.context, 'init', {});
    if (response.success) {
      return response.result as void;
    } else {
      throw new Error(response.error || 'Execution failed');
    }
  }

  /**
   * add_secret
   */
  public async addSecret(params: { name: string; secret_type: string; data: string; tags: string[] }): Promise<string> {
    const response = await this.app.execute(this.context, 'add_secret', params);
    if (response.success) {
      return response.result as string;
    } else {
      throw new Error(response.error || 'Execution failed');
    }
  }

  /**
   * update_secret
   */
  public async updateSecret(params: { secret_id: string; name: string; data: string; tags: string[] }): Promise<void> {
    const response = await this.app.execute(this.context, 'update_secret', params);
    if (response.success) {
      return response.result as void;
    } else {
      throw new Error(response.error || 'Execution failed');
    }
  }

  /**
   * delete_secret
   */
  public async deleteSecret(params: { secret_id: string }): Promise<void> {
    const response = await this.app.execute(this.context, 'delete_secret', params);
    if (response.success) {
      return response.result as void;
    } else {
      throw new Error(response.error || 'Execution failed');
    }
  }

  /**
   * get_secret
   */
  public async getSecret(params: { secret_id: string }): Promise<SecretItem> {
    const response = await this.app.execute(this.context, 'get_secret', params);
    if (response.success) {
      return response.result as SecretItem;
    } else {
      throw new Error(response.error || 'Execution failed');
    }
  }

  /**
   * list_secrets
   */
  public async listSecrets(): Promise<SecretItem[]> {
    const response = await this.app.execute(this.context, 'list_secrets', {});
    if (response.success) {
      return response.result as SecretItem[];
    } else {
      throw new Error(response.error || 'Execution failed');
    }
  }

  /**
   * search_secrets
   */
  public async searchSecrets(params: { query: string }): Promise<SecretItem[]> {
    const response = await this.app.execute(this.context, 'search_secrets', params);
    if (response.success) {
      return response.result as SecretItem[];
    } else {
      throw new Error(response.error || 'Execution failed');
    }
  }

  /**
   * get_secrets_by_tag
   */
  public async getSecretsByTag(params: { tag: string }): Promise<SecretItem[]> {
    const response = await this.app.execute(this.context, 'get_secrets_by_tag', params);
    if (response.success) {
      return response.result as SecretItem[];
    } else {
      throw new Error(response.error || 'Execution failed');
    }
  }

  /**
   * get_audit_logs
   */
  public async getAuditLogs(): Promise<AuditLogEntry[]> {
    const response = await this.app.execute(this.context, 'get_audit_logs', {});
    if (response.success) {
      return response.result as AuditLogEntry[];
    } else {
      throw new Error(response.error || 'Execution failed');
    }
  }

}