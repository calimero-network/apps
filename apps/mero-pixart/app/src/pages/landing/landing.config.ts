/**
 * Landing content for Mero PixArt.
 *
 * GENERATED by `pnpm landing:generate` from scripts/landing/apps.config.mjs
 * plus this app's [package.metadata.calimero] table. Do not edit by hand —
 * `pnpm landing:check` fails CI on drift. Change the copy in apps.config.mjs.
 *
 * `name`, `packageId` and `tagline` come from the Cargo metadata the registry
 * publishes from, so this page and the registry listing cannot diverge.
 */
import { Circle, Cube3D, Cube3DLayers, Eye, LineChart, Zap } from '@calimero-network/mero-icons';

import type { LandingConfig } from './landingTypes';

export const CONFIG: LandingConfig = {
  name: "Mero PixArt",
  packageId: "com.calimero.mero-pixart",
  tagline: "Collaborative p2p image editor on the Calimero network. Photoshop-style layers, masks, and adjustments — your pixels on your nodes.",
  dir: "mero-pixart",
  iconSrc: '/icon-512.png',
  availability: "web+desktop",
  trust: ["Non-destructive edits", "Real layer folders", "Pixels on your nodes"],
  explainer: [
    "A collaborative raster image editor with the depth of a desktop tool — layer folders, masks, blend modes, curves and a corner-pin warp — that several people can work in at the same time.",
    "Adjustments are non-destructive and applied live, so you can reconsider an exposure change a week later. The project, including every uploaded image, lives as blobs on your node rather than in a cloud editor’s bucket.",
  ],
  features: [
    {
      icon: Cube3DLayers,
      title: "Layers and folders",
      body: "Raster, text and fill layers inside real collapsible folders, not a flat list pretending to nest.",
    },
    {
      icon: Zap,
      title: "Non-destructive adjustments",
      body: "Brightness, contrast, saturation, hue, exposure, blur and invert, applied live and always reversible.",
    },
    {
      icon: LineChart,
      title: "Curves",
      body: "A per-channel RGB spline editor applied through a lookup table, the way a real editor does it.",
    },
    {
      icon: Eye,
      title: "Blend modes and masks",
      body: "All sixteen blend modes, per-layer opacity, and paintable masks to hide or reveal.",
    },
    {
      icon: Cube3D,
      title: "Free transform",
      body: "Move, scale, rotate, shear, mirror, and a corner-pin warp for perspective.",
    },
    {
      icon: Circle,
      title: "Paint tools",
      body: "Brush, eraser, bucket fill and eyedropper, each re-rendering the layer to a new blob.",
    },
  ],
};
