# Generated, but COMMITTED on purpose: CI regenerates and diffs it, which is
# what turns a contract/frontend mismatch into a failed build. Marked so it
# collapses in review — the diff that matters is the ABI it came from.
KvStoreClient.ts linguist-generated=true
