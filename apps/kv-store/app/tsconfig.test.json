{
  "// note": "Unit tests. Node types on purpose: generated.test.ts reads logic/res/abi.json off disk to assert the client covers the contract.",
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist-types/test",
    "tsBuildInfoFile": "./dist-types/test.tsbuildinfo",
    "types": ["node", "vite/client"]
  },
  "include": ["src/**/*.test.ts", "src/**/*.test.tsx", "src/test", "src/generated"]
}
