// Types for workload.mjs, so src/lib/workloadParity.test.ts can import it under
// the app's tsconfig. The .mjs stays plain JS: it is imported directly by node.
export declare const KEYFRAME_COST_RATIO: number;
