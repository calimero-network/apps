{
  "// note": "Playwright specs and their harness. Node types: global-setup spawns merod and reads the bundle off disk.",
  "// exclude": [
    "`marketing-landing.spec.ts` is GENERATED by `pnpm landing:generate` from the",
    "fleet-wide template, and it is written against the looser per-app tsconfigs",
    "every other app that renders the landing page uses — under this repo's shared",
    "strict base it fails on `page.locator(href)` with a nullable href. Editing it",
    "is not an option (`pnpm landing:check` fails CI on drift), and Playwright",
    "transpiles rather than typechecks, so it runs regardless. Excluded here so the",
    "specs this app OWNS are still typechecked."
  ],
  "extends": "../../../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "../dist-types/e2e",
    "tsBuildInfoFile": "../dist-types/e2e.tsbuildinfo",
    "types": ["node"]
  },
  "include": ["."],
  "exclude": ["marketing-landing.spec.ts"]
}
