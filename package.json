{
  "name": "@calimero-network/apps",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.6.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "codegen": "pnpm -r codegen",
    "check:metadata": "bash scripts/check-app-metadata.sh"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
